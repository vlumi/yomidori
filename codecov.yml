# Coverage tracks the testable logic, not the SwiftUI view layer (which needs
# UI automation this project doesn't run) nor the camera/OCR glue (which needs
# a device). The split is by Swift target: YomidoriCore is pure, headlessly-
# testable logic and is gated; YomidoriKit is the SwiftUI/Vision/UIKit glue and
# is ignored wholesale. Keep logic in YomidoriCore so it's covered automatically
# — no per-file edits here when a view file is added or split for the lint
# length limit.

coverage:
  status:
    project:
      default:
        target: auto          # don't drop below the current project coverage
        threshold: 2%         # tolerate small fluctuations
    patch:
      default:
        target: 80%           # new (non-ignored) code must be ≥80% covered
        threshold: 0%         # no slack below the target

comment:
  # Only comment when coverage actually changed; keeps PRs quiet.
  require_changes: true

ignore:
  - "Packages/YomidoriCore/Sources/YomidoriKit/**"
  - "Packages/YomidoriCore/Tests/**"
