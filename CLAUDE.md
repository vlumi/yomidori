# CLAUDE.md

Project guidance for AI coding agents lives in [AGENTS.md](AGENTS.md), which is
the single canonical source. Claude Code reads this file too — everything it
needs is in AGENTS.md, so keep that one current rather than duplicating here.
